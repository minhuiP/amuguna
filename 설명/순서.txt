1. 플라스크, 파이썬 다운로드
2. chat gdt 사용법 익히기
3. 파이썬 html 연동시키기? (아마 플라스크 설정에 문제 있는듯)- app.py 없었고. html 위치가 템플릿츠에 있어야했음


4. 파이썬과 sqlite3 연동._ 완성!

5.
파이썬 파일에서 sqlite3를 데이터베이스로 쓰는 amuguna.db의 항목인 food_name 중 하나를 랜덤으로 꺼내줘.
food_name은 flask를 index.html에 보여지면 좋겠어.

우선 내 로컬 컴퓨터에서만 보여질 수 있도록 해줘.
 

6. 파이썬을 이용해 데이터베이스에서 랜덤으로 음식이름 뽑게.

7. 과정 반복

sqlite3의 amuguna.db에서 랜덤으로 꺼낸 food_name의 항목이 tag1,tag2,tag3,tag4 에서 아무거나 랜덤으로 꺼내줘.
그걸 index.t=html의 버튼에 보일수 있게 해줘. 


The item of food_name randomly pulled out from amuguna.db of sqlite3 is randomly pulled out from tag1, tag2, tag3, tag4.
Make it visible on the button at index.html.


my mean is pulled out from column tag1, tag2, tag3, tag4 
태그의 종류를 종류별로 꺼내고 싶지만. 지금 나로서는 그걸 해결하기 어려워. 
이유는 대표적으로 한가지만 꺼내자. 

근데 해냄. ㅋ


8.

1) {food_tag} 버튼을 클릭하면, 선택값과 동일한 value를 가진 항목은 제외하고 food_name을 랜덤으로 다시 화면에 보여준다.
{food_tag}도 기존처럼 

When you click the {food_tag} button, the food_name is randomly displayed again, excluding items with the same value as the value.
{food_tag} as before
to randomly assign one of the values from the columns tag1, tag2, tag3, tag4 to the randomly pulled out food_name from the amuguna.db database
Repeat this process each time you click the button.
Pressing refresh will reset this process.
When there is no food_name that can be displayed, all buttons and letters disappear and the text "Do not eat" is displayed on the screen.
버튼을 클릭할 때 마다 이 과정을 반복한다. 
새로고침을 누르면 이 과정은 초기화 된다.
보여줄 수 있는 food_name이 없어지면 모든 버튼과 글씨가 사라지고 "먹지마" 란 글씨가 화면에 보여진다.

index.html 은 다음과 같은 화면을 보여준다.

food_name 

버튼1은 오른쪽에 있는 "선택"이 써있다, 버튼2는 food_name 의  columns tag1, tag2, tag3, tag4 한 값을 랜덤으로 보여준다.



화면엔 

food_name 글자

그 밑에 버튼 두개만 있다.

첫번째 버튼엔 "선택"이라 써있고, 두번째 버튼에는 food_name의 tag1,tag2,tag3,tag4중에 한 가지 값만 들어있다. 


index.html의 food_tag버튼을 누르면, 화면의 구성은 그대로이지만 food_name과 food_tag는 이전의 방식처럼 랜덤하게 돌아간다.
(단,  tag1,tag2,tag3,tag4 중 클릭했던 food_tag와 같은 값을 가진 food_name은 다신 보여지지 않는다.)
클릭 할 때마다, 보여줄 수 있는 food_name이 없어지면 
"먹지마"란 글자만 화면에 뜬다.


please edit
When the food_tag ​​button in index.html is pressed, the composition of the screen remains the same, but the food_name and food_tag ​​rotate randomly as before.
(However, among tag1, tag2, tag3, and tag4, the food_name with the same value as the clicked food_tag ​​will not be displayed again.)
Every time I click, when there is no food_name to show
Only the words "Don't eat" appear on the screen.

내 뜻은 중복되는 tag를 제외 한 food_name만 보여줘
I mean only show food_name excluding duplicate tag